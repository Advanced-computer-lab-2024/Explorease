import React from 'react';

const AdminDashboard = () => {
    return (
        <div>
            <h1>Welcome to the Admin Dashboard</h1>
            {/* Add more admin features here, such as links to manage users, products, etc. */}
        </div>
    );
};

export default AdminDashboard;
